server:
  port: 8001
spring:
  application:
    name: chess-user-service
  datasource:
    url: jdbc:mysql://localhost:3306/chese
    username: root
    password: 123456
  redis:
    host: localhost
  rabbitmq:
    host: 120.78.94.125
    port: 5672
    username: guest
    password: guest
    virtual-host: /
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka/
  instance:
    prefer-ip-address: true
    ip-address: localhost
mybatis:
  type-aliases-package: com.chess.user.pojo
logging:
  level:
    root: info
ribbon:
  eager-load:
    enabled: true